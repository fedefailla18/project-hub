# Unified Task Board

Global priorities across the workspace.

## 🔴 High Priority
- **`agents`**: Sync `main` branch with remote origin (33 commits behind).
- **Workspace**: Deploy and monitor the new unified Docker orchestration (`docker-compose.yml`).
- **`importer-porfolio` [2 PRs OPEN, awaiting review/merge]**:
  - [PR #10](https://github.com/fedefailla18/importer-porfolio/pull/10) — added the missing `Dockerfile` (+ `nginx.conf`, `.dockerignore`) so `crypto-ui` can build. Verified standalone and through the full `project-hub` compose stack. Branch hygiene: closed the long-stale [PR #9](https://github.com/fedefailla18/importer-porfolio/pull/9) (`chore/migrate-to-pnpm-and-security-updates`, 14 commits behind `main`, superseded by `main`'s own independent pnpm migration) and deleted its branch plus `docs-ui-audit-and-stats-polish` (confirmed via `git patch-id` already squash-merged into `main` under a different hash).
  - [PR #11](https://github.com/fedefailla18/importer-porfolio/pull/11) — `SyncJobsPanel` companion to investracker PR #65: live per-data-type sync status (chunk progress, retry on FAILED), polling every 4s; `BinanceSyncDialog` stays open after triggering instead of closing on a single toast. **Updated 2026-09-16**: sync-button visibility now requires an exact `exchangeName` match (dropped a fallback that also matched manually-managed portfolios by name substring); added a Consolidate dialog + a dismissible Portfolio-vs-Exchanges guide on the two landing pages — companion to investracker PR #65's backend hardening below.
- **`project-hub` [PR OPEN, awaiting review/merge]**: [PR #1](https://github.com/fedefailla18/project-hub/pull/1) — `wp` no longer streams raw service logs; quiet status lines instead, full log only on failure. Also fixed Ctrl+C leaving gradlew's JVM running, and `run_all()` missing `crypto-ui`.
- **`investracker` [3 PRs OPEN, awaiting review/merge]** (repo renamed `file-importer` → `investracker` on GitHub 2026-09-12 — links below use the new URL, old ones auto-redirect):
  - [PR #63](https://github.com/fedefailla18/investracker/pull/63) — renamed the project everywhere except the Java package (`com.importer.fileimporter`) and DB schema (`file_importer_schema`), deliberately left alone (real cost, zero user-facing benefit). Reorganized 20 markdown docs down to 8, by feature/use-case instead of by author/date — an agent read all 20 in full and cross-checked claims against real code first. Found and fixed several stale/wrong facts along the way: dead `TransactionProcessor` references (removed by PR #60, three docs still described it as current), an incomplete `stableTotalCost`→`inventoryCostUsdt` rename presented as done, `GET /api/holdings` wrong in 4 docs (real path `/holding`), a fabricated JWT response field name, wrong port in the auth guide, and `CLAUDE.md` mischaracterizing IOL's client as WebClient-based (it's Feign).
  - [PR #64](https://github.com/fedefailla18/investracker/pull/64) — `BinanceIntegrationController`'s `/my-trades` and `/orders` proxy endpoints 400'd on their own default date range (2020→now): windowed into 180-day chunks calling Binance with both `startTime` and `endTime` set, which Binance rejects past 24h apart (confirmed against Binance's own docs — long-standing constraint, not a recent change). Fixed to page by id instead, matching the pattern already used correctly elsewhere in this codebase. Also fixed `/deposits`/`/withdrawals` (180-day window where Binance's real limit is 90 days). Added the controller's first-ever test coverage.
  - [PR #65](https://github.com/fedefailla18/investracker/pull/65) — resumable, chunked job tracking for the Binance full-history sync (`/transaction/sync/binance/full`), replacing the old fire-and-forget single-`@Transactional` design. New `sync_job`/`sync_job_chunk` tables (one job per data type, one chunk per symbol or date window), no `@Transactional` anywhere in the new path (every save commits on its own — that's the actual fix, not finer-grained transactions), retry re-runs only FAILED chunks, `TransactionService.saveIfAbsent` now catches the DB constraint violation instead of surfacing a 500 under concurrent retries. Deliberately does not touch the separate, live `binance_sync_progress`/`BinanceOrderSyncService` diagnostic feature. Full spec in `investracker/docs/binance-sync-jobs.md`, written before implementation. Companion FE PR: [importer-porfolio #11](https://github.com/fedefailla18/importer-porfolio/pull/11) (`SyncJobsPanel` — live per-type status + retry, polling every 4s).
    **Updated 2026-09-16 — Portfolio vs Exchanges**: the user caught a gap this PR's original design missed — every sync path (this one included) wrote into whatever portfolio name was passed, with nothing stopping it from silently relabeling the user's manually-managed portfolio as exchange-owned. Fixed: `PortfolioService.resolveExchangePortfolio(name, exchangeName)` — every sync entry point (Binance/MexC, incremental and full) now goes through it instead of `findOrSave`; throws `PortfolioNotExchangeOwnedException` (400) if asked to target a manual or wrong-exchange portfolio. Also fixed a related bug found along the way: `BinanceSyncService`/`MexcSyncService`'s incremental trades were never tagged with `exchangeName`/`externalId` (only deposits/withdrawals were), making them indistinguishable from manual entries. New `POST /portfolio/consolidate` (+ `GET /portfolio/mine`) is the explicit, repeat-safe action that merges an exchange portfolio's transactions into a manual one. Docs: new "Portfolio vs Exchanges" section in `architecture.md`, `exchange-integrations.md`/`CLAUDE.md` updated to match. Full suite: 137 tests, 11 failures — same pre-existing set as before (2 fewer than the prior count — fixed a stale mock mismatch in `PortfolioServiceSpec` while rewriting it), 0 new failures.
    **Updated 2026-09-23 — orphaned-job self-healing**: confirmed in the user's own DB — a full sync batch from 2026-09-16 left all 5 jobs stranded `PENDING`/`RUNNING` (4 never started, `TRADES` was mid-flight) when the backend restarted seconds after being triggered, with no way to retry them (retry only accepts `FAILED`). Fixed with `BinanceFullSyncService.reconcileOrphanedJobs()`, run once on `ApplicationReadyEvent` via new `SyncJobStartupReconciler` — any job found `PENDING`/`RUNNING` right after boot can only be orphaned (this process couldn't have created it), gets marked `FAILED` with `RUNNING` chunks reset to `PENDING` (`COMPLETED` chunks/checkpoints untouched), ready for the existing retry flow. Does not auto-retry on boot, deliberately. 2 new tests, 139 total, same 11 pre-existing failures, 0 new. MexC's full sync (still fire-and-forget) isn't covered by this fix.
    **Updated 2026-09-16 (later same day) — live-blocking bug reported by user**: `GET /portfolio` threw `SQLGrammarException: column holdings0_.inventory_cost_usdt does not exist` for every portfolio — `Holding.inventoryCostUsdt` (Java) never had its DB column actually renamed from the old `stable_total_cost` (a dangling piece of the incomplete rename PR #63 already flagged in docs). Migration 045 renames the column. Migration 046 backfills `exchange_name` on existing transactions left untagged by the incremental-sync bug fixed above — safe, since a transaction inside an already exchange-owned portfolio can only have come from that exchange's sync. User also asked where the "fetch from Binance" button was on the Binance Activity page — there wasn't one; that page only ever re-displayed already-synced DB data + live balances, never triggered a sync itself. Added a "Sync from Binance"/"Sync from MexC" button to both activity pages (companion FE commit). **User needs to restart the backend (`./gradlew bootRun`) to pick up migrations 045/046** — not yet confirmed fixed on their end.
    **Updated 2026-09-23 (later same day) — `LazyInitializationException` on retry, root-caused and fixed**: after the orphaned-job reconciler above marked the 2026-09-16 batch `FAILED` and the user retried it, every chunk of the `TRADES` job (and one `DEPOSITS` chunk) failed with `could not initialize proxy [...Portfolio#...] - no Session`. Root cause: `runJob(UUID jobId)` — used by both the initial trigger and `retryJob` — reloads the job via plain `syncJobRepository.findById(jobId)`, which returns `job.getPortfolio()` as a *lazy* proxy (`SyncJob.portfolio` is `@ManyToOne(LAZY)`). Every chunk runner calls `portfolio.getUser()` (via `rawResponseService.saveResponse(...)`), which requires Hibernate to initialize that proxy — but by design this codebase has no `@Transactional` spanning `runJob` (deliberate, see PR #65's per-chunk-commit design), so the session from the `findById` call is already closed by then. This is distinct from job *creation* (`createSyncJobs`), which passes in the already-fully-loaded `Portfolio` from `portfolioService.resolveExchangePortfolio(...)` directly — never hit this path. Likely never surfaced before because the 2026-09-16 batch died within ~2s of being triggered, before/while the first chunk ran; today's retry was the first time `runJob` actually got to process real chunks. **Fixed** (keeps the no-spanning-transaction design intact): added `SyncJobRepository.findByIdWithPortfolioAndUser` — a `JOIN FETCH`-based query that eagerly loads `job.portfolio` + `portfolio.user` in the one query/session, so they're fully populated (not proxies) by the time `runJob` uses them, with no need for an open session afterward. `runJob` now uses it instead of plain `findById`. Full suite: 139 tests, same 11 pre-existing failures, 0 new. Pushed to PR #65, not yet confirmed fixed against the user's real batch (needs a backend restart + retry to verify).

## 🟡 Medium Priority
- **`wealthtrack`**: Monitor Vercel deployment of the `feature/optimize-data-and-trends` branch.
- **`healthVault`**: Begin Phase 2 (Google Sheets Integration) research.
- **`project-hub`**: Create symbolic links to project `GEMINI.md` files for faster context switching.
- **`cv-generator`**: Refactor legacy themes (Modern/Minimal/Compact/Two-Column) onto the shared components the Engineering theme introduced (`src/themes/shared/`, `cvRenderHelpers.ts`).

## 🟢 Low Priority
- **Security**: Run periodic scans for new vulnerabilities in the pnpm ecosystem.

## ✅ Completed
- **`project-hub`**: Model tier logic updated to zero-cost local runner (`ai/gemma3`), pycache ignored, branch `feature/create-agents` merged into `main`. Docker orchestration added (`docker-compose.yml`).
- **`wealthtrack`**: Merged debt support & account panel branch (`cr/feat/debt-support-and-account-settings`) into `main`. Cleaned up stale SSO branch. All 102 unit tests passing. Migrated to pnpm + Docker multi-stage build.
- **`cv-generator`**: Extracted sticky `Navbar` component, added candidate/job profiles, resolved TS2322 in `InterviewsDashboard`, build verified, and merged `feature/data-persistence` into `main`.
- **`cv-generator`**: Migrated to pnpm; added `CLAUDE.md` and mirrored the `senior-interviewer` + new `master-fe-requirement-cv-template-translator` skills under `.claude/skills/` (parity with existing `.gemini/skills/`). Added an **Engineering (Senior)** CV theme (paired with content spec `upgrade.md`). Fixed a critical bug where 26 stale compiled `.js` files committed under `src/` were silently shadowing their `.tsx`/`.ts` sources in both dev and prod builds (Vite resolves `.js` before `.tsx` for extensionless imports) — deleted them and set `tsconfig.json` `noEmit: true` to prevent recurrence. Fixed a render crash when `WorkItem.technologies` is a flat array instead of a grouped record, and added support for a `domains` field. Clarified `interviews/README.md` wording (`interviews/jobs/` = roles you're hiring for, not your own job search). Deleted stale local branch `fix/error-handling` (fully superseded by `main`, would have reintroduced the `.js` files and deleted newer candidate data).
- **`healthVault`**: Committed Phase 2 Google Sheets integration plan (`PHASE2_PLAN.md`) on `main`.
- **`importer-porfolio`**: Fixed `App.test.tsx` assertion, built successfully, and merged exchange sync UI branch `docs-ui-audit-and-stats-polish` into `main`. Migrated to pnpm. **Correction (2026-09-08): the "Docker Nginx runner" part of this was never actually true — there is no `Dockerfile` in this repo (see new `🔴 High Priority` entry above). Leaving this line as historical record, but don't trust it as a fact about current state.**
- **`wealthtrack`**: Fixed "Sync Data" only refreshing the Dashboard (added `revalidatePath()` for every sheet-data route in `lib/actions.ts`, keeping the 24h passive cache TTL — user's call). Fixed dates from the Apps Script snapshot automation rendering as raw serial numbers (`46258` etc.) instead of text on `/balance` and Registro_Saldos-derived screens — added `formatDateDisplay()` (`lib/utils/date-utils.ts`) and wired it into `balance-transformers.ts` + `registro-saldos-transformers.ts`. Mirrored the Google Apps Script locally (`apps-script/wealthtrack-core.gs.js`). All 102 tests + build passing.
- **`investracker`**: Merged [PR #61](https://github.com/fedefailla18/investracker/pull/61) (upload 415 fix + CORS both ports) and [PR #62](https://github.com/fedefailla18/investracker/pull/62) (Binance/MexC "no results" bug — `QUOTE_CURRENCIES` wrongly excluded BTC/ETH/BNB/MX from sync; IOL auth failures logging users out of the whole app — now remapped 424/502; MexC `/account` 400 logging improved, root cause still external/unconfirmed).
- **`investracker`/`project-hub`**: Fixed `./wp run crypto-tracker`/`./wp run docker` "Network Error" — `wp` and `project-hub/docker-compose.yml` both pointed at a dead `../file-importer` stub instead of `../investracker` (local dir renamed, GitHub repo still `file-importer`), plus a wrong port (8080 vs the real 9080) and a missing `?currentSchema=file_importer_schema,public` on the compose datasource URL. Rewrote `investracker/Dockerfile` as a proper multi-stage build (compiles from source; also fixed amd64-only base images so it now builds natively on Apple Silicon) — verified with a real `docker build` + `docker run` against the actual dev DB, and again through the corrected `docker compose up crypto-db crypto-redis crypto-api`, both fully booting and serving on 9080. Retired the orphaned, always-empty `crypto-db`/`file-importer` Postgres instance (verified 0 tables before deleting) and unified `project-hub`'s `crypto-db` onto investracker's real data directory/port (5435) — the two docker-compose files now manage the *same* database, so only run one of their postgres services at a time. Also fixed an unrelated copy-paste bug in `wp`'s `run_cv_gen()` (was launching healthVault instead of cv-generator) and wrong `HEALTH_VAULT_DIR`/`CV_GEN_DIR` paths. Corrected root `CLAUDE.md`'s stale investracker port docs (8080→9080) and its recommendation to use the destructive `start-db.sh` as a default quick-start step (should be the non-destructive `docker-compose up -d`, per investracker's own accurate `CLAUDE.md`).

---
*Last Updated: 2026-09-16*
